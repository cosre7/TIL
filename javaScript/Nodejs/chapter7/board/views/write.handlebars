{{!-- mode에 따라 문구 변경  --}}
<h1>[{{title}}] 글 {{#if (eq mode "create")}}작성{{else}}수정{{/if}}</h1>

<div>
    {{!-- 글쓰기 폼 --}}
    {{!-- mode 에 따라 폼이 반영되는 URL 변경 --}}
    <form name="boardForm" method="post" {{#if (eq mode "create")}}action="/write" {{else}}action="/modify" {{/if}}>

        {{!-- 수정 모드인 때는 id 값을 폼을 통해 전송 --}}
        {{#if (eq mode 'modify')}}
        <input type="hidden" name="id" value="{{post._id}}">
        {{/if}}
        {{!-- 제목 입력 칸 --}}
        <div>
            <label>제목</label>
            <input type="text" name="title" placeholder="제목을 넣어주세요" value="{{post.title}}">
        </div>
        {{!-- 이름 입력 칸 --}}
        <div>
            <label for="">이름</label>
            <input type="text" name="writer" placeholder="이름을 넣어주세요" value="{{post.writer}}">
        </div>
        {{!-- 비밀번호 입력 칸 --}}
        <div>
            <label for="">비밀번호</label>
            <input type="text" name="password" placeholder="비밀번호를 넣어주세요">
        </div>

        {{!-- 본문 입력 --}}
        <div>
            <label for="">본문을 입력하세요</label><br>
            <textarea name="content" id="" cols="50" rows="10" placeholder="본문">{{post.content}}</textarea>
            <br>
        </div>
        {{!-- 버튼 영역 --}}
        <div>
            <button type="submit">저장</button>
            <button type="button" onclick="location.href='/'">취소</button>
        </div>
    </form>
</div>